export interface BasicArrowProp {
  /**
   * 箭头展开状态
   */
  expand: boolean;
  /**
   * 默认向上箭头
   */
  up: boolean;
  /**
   * 默认向下箭头
   */
  down: boolean;
  /**
   * 取消内联的内边距/边距
   */
  inset: boolean;
}
